<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 50 %>
      <%= user.display_name %>
      <%= link_to '@'+user.username, user %>
    </li>
  <% end %>
</ul>

